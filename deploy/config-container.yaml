server:
  port: 8888

postgres:
  dsn: "postgres://clipanvil:clipanvil_dev@postgres:5432/clipanvil?sslmode=disable"

redis:
  addr: "redis:6379"

minio:
  endpoint: "minio:9000"
  access_key: "clipanvil"
  secret_key: "clipanvil_dev"
  use_ssl: false
